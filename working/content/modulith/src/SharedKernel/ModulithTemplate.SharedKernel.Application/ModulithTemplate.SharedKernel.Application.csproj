<Project Sdk="Microsoft.NET.Sdk">

  <!-- Abstractions every feature's Application layer implements or injects: the two event-handler
       interfaces, the published-contract marker, and the queue handlers enqueue onto.

       Abstractions only — the implementations live in Shared.Infrastructure. -->
  <ItemGroup>
    <PackageReference Include="Mediator.Abstractions" />
    <PackageReference Include="FluentResults" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../ModulithTemplate.SharedKernel.Domain/ModulithTemplate.SharedKernel.Domain.csproj" />
  </ItemGroup>

</Project>
